version: 1
base_plan: "./.skill-plan.yaml"
generated_at: "2026-05-30T00:30:00Z"
source_reports:
  - "./squash-skill-check-report.html"
  - "./squash-iteration-5-eval-report.html"
operations:
  - id: "op-001"
    priority: "P0"
    source:
      report_type: "skill-check"
      finding_id: "spec:when_to_load_reference_files"
    action: "replace"
    path: "workflow_notes"
    before: []
    after:
      - "Load assets/context-envelope.schema.json when validating envelope keys/version before unpack output is trusted."
      - "Run deterministic checks in scripts first; only then emit restored context block."
    rationale: "Adds explicit reference-load trigger and validation ordering required by spec checks."
    status: "approved"
  - id: "op-002"
    priority: "P2"
    source:
      report_type: "skill-check"
      finding_id: "bp:no explicit when-to-load guidance for reference files"
    action: "replace"
    path: "workflow_notes"
    before:
      - "Load assets/context-envelope.schema.json when validating envelope keys/version before unpack output is trusted."
      - "Run deterministic checks in scripts first; only then emit restored context block."
    after:
      - "Load assets/context-envelope.schema.json when validating envelope keys/version before unpack output is trusted."
      - "Load references/restore-playbook.md when unpack returns status error or schema/hash checks fail."
      - "Run deterministic checks in scripts first; only then emit restored context block."
    rationale: "Adds explicit failure-path trigger for deeper reference guidance."
    status: "approved"
  - id: "op-003"
    priority: "P2"
    source:
      report_type: "skill-check"
      finding_id: "bp:no provenance note from real execution iteration"
    action: "replace"
    path: "workflow_notes"
    before:
      - "Load assets/context-envelope.schema.json when validating envelope keys/version before unpack output is trusted."
      - "Load references/restore-playbook.md when unpack returns status error or schema/hash checks fail."
      - "Run deterministic checks in scripts first; only then emit restored context block."
    after:
      - "Load assets/context-envelope.schema.json when validating envelope keys/version before unpack output is trusted."
      - "Load references/restore-playbook.md when unpack returns status error or schema/hash checks fail."
      - "Run deterministic checks in scripts first; only then emit restored context block."
      - "Record provenance notes from eval iterations (e.g., baseline mismatch, assertion drift, telemetry gaps) in workflow guidance."
    rationale: "Persists run-derived evidence expectations at plan level."
    status: "approved"
  - id: "op-004"
    priority: "P3"
    source:
      report_type: "skill-eval"
      finding_id: "pattern:always-pass"
    action: "replace"
    path: "workflow_notes"
    before:
      - "Load assets/context-envelope.schema.json when validating envelope keys/version before unpack output is trusted."
      - "Load references/restore-playbook.md when unpack returns status error or schema/hash checks fail."
      - "Run deterministic checks in scripts first; only then emit restored context block."
      - "Record provenance notes from eval iterations (e.g., baseline mismatch, assertion drift, telemetry gaps) in workflow guidance."
    after:
      - "Load assets/context-envelope.schema.json when validating envelope keys/version before unpack output is trusted."
      - "Load references/restore-playbook.md when unpack returns status error or schema/hash checks fail."
      - "Run deterministic checks in scripts first; only then emit restored context block."
      - "Record provenance notes from eval iterations (e.g., baseline mismatch, assertion drift, telemetry gaps) in workflow guidance."
      - "Prioritize discriminative eval assertions (exact_json/ordered_markers/regex_match) and prune always-pass checks each iteration."
    rationale: "Encodes ongoing eval-signal maintenance in plan workflow."
    status: "approved"
  - id: "op-005"
    priority: "P3"
    source:
      report_type: "skill-eval"
      finding_id: "fix:token telemetry estimated"
    action: "replace"
    path: "workflow_notes"
    before:
      - "Load assets/context-envelope.schema.json when validating envelope keys/version before unpack output is trusted."
      - "Load references/restore-playbook.md when unpack returns status error or schema/hash checks fail."
      - "Run deterministic checks in scripts first; only then emit restored context block."
      - "Record provenance notes from eval iterations (e.g., baseline mismatch, assertion drift, telemetry gaps) in workflow guidance."
      - "Prioritize discriminative eval assertions (exact_json/ordered_markers/regex_match) and prune always-pass checks each iteration."
    after:
      - "Load assets/context-envelope.schema.json when validating envelope keys/version before unpack output is trusted."
      - "Load references/restore-playbook.md when unpack returns status error or schema/hash checks fail."
      - "Run deterministic checks in scripts first; only then emit restored context block."
      - "Record provenance notes from eval iterations (e.g., baseline mismatch, assertion drift, telemetry gaps) in workflow guidance."
      - "Prioritize discriminative eval assertions (exact_json/ordered_markers/regex_match) and prune always-pass checks each iteration."
      - "Capture real token telemetry in timing artifacts when available; label estimates explicitly when telemetry is unavailable."
    rationale: "Improves benchmark reliability by distinguishing measured vs estimated token costs."
    status: "approved"
  - id: "op-006"
    priority: "P2"
    source:
      report_type: "skill-check"
      finding_id: "bp:mode-coupled-wording"
    action: "replace"
    path: "workflow_notes"
    before:
      - "Load assets/context-envelope.schema.json when validating envelope keys/version before unpack output is trusted."
      - "Load references/restore-playbook.md when unpack returns status error or schema/hash checks fail."
      - "Run deterministic checks in scripts first; only then emit restored context block."
      - "Record provenance notes from eval iterations (e.g., baseline mismatch, assertion drift, telemetry gaps) in workflow guidance."
      - "Prioritize discriminative eval assertions (exact_json/ordered_markers/regex_match) and prune always-pass checks each iteration."
      - "Capture real token telemetry in timing artifacts when available; label estimates explicitly when telemetry is unavailable."
    after:
      - "Load assets/context-envelope.schema.json when validating envelope keys/version before unpack output is trusted."
      - "Load references/restore-playbook.md when unpack returns status error or schema/hash checks fail."
      - "Run deterministic checks in scripts first; only then emit restored context block."
      - "Record provenance notes from eval iterations (e.g., baseline mismatch, assertion drift, telemetry gaps) in workflow guidance."
      - "Prioritize discriminative eval assertions (exact_json/ordered_markers/regex_match) and prune always-pass checks each iteration."
      - "Capture real token telemetry in timing artifacts when available; label estimates explicitly when telemetry is unavailable."
      - "Keep generated instructions mode-neutral; avoid style-coupled phrases unless capability depends on them."
    rationale: "Prevents style-mode coupling from leaking into core skill instructions."
    status: "approved"
  - id: "op-007"
    priority: "P3"
    source:
      report_type: "skill-eval"
      finding_id: "fix:token-telemetry-source"
    action: "replace"
    path: "workflow_notes"
    before:
      - "Load assets/context-envelope.schema.json when validating envelope keys/version before unpack output is trusted."
      - "Load references/restore-playbook.md when unpack returns status error or schema/hash checks fail."
      - "Run deterministic checks in scripts first; only then emit restored context block."
      - "Record provenance notes from eval iterations (e.g., baseline mismatch, assertion drift, telemetry gaps) in workflow guidance."
      - "Prioritize discriminative eval assertions (exact_json/ordered_markers/regex_match) and prune always-pass checks each iteration."
      - "Capture real token telemetry in timing artifacts when available; label estimates explicitly when telemetry is unavailable."
      - "Keep generated instructions mode-neutral; avoid style-coupled phrases unless capability depends on them."
    after:
      - "Load assets/context-envelope.schema.json when validating envelope keys/version before unpack output is trusted."
      - "Load references/restore-playbook.md when unpack returns status error or schema/hash checks fail."
      - "Run deterministic checks in scripts first; only then emit restored context block."
      - "Record provenance notes from eval iterations (e.g., baseline mismatch, assertion drift, telemetry gaps) in workflow guidance."
      - "Prioritize discriminative eval assertions (exact_json/ordered_markers/regex_match) and prune always-pass checks each iteration."
      - "Capture real token telemetry in timing artifacts when available; label estimates explicitly when telemetry is unavailable."
      - "Keep generated instructions mode-neutral; avoid style-coupled phrases unless capability depends on them."
      - "Require eval harness to record both total_tokens and token_source (telemetry or estimate) for each run."
    rationale: "Makes token-cost data quality explicit and machine-auditable."
    status: "approved"
  - id: "op-008"
    priority: "P3"
    source:
      report_type: "skill-eval"
      finding_id: "pattern:always-pass-minimization"
    action: "replace"
    path: "workflow_notes"
    before:
      - "Load assets/context-envelope.schema.json when validating envelope keys/version before unpack output is trusted."
      - "Load references/restore-playbook.md when unpack returns status error or schema/hash checks fail."
      - "Run deterministic checks in scripts first; only then emit restored context block."
      - "Record provenance notes from eval iterations (e.g., baseline mismatch, assertion drift, telemetry gaps) in workflow guidance."
      - "Prioritize discriminative eval assertions (exact_json/ordered_markers/regex_match) and prune always-pass checks each iteration."
      - "Capture real token telemetry in timing artifacts when available; label estimates explicitly when telemetry is unavailable."
      - "Keep generated instructions mode-neutral; avoid style-coupled phrases unless capability depends on them."
      - "Require eval harness to record both total_tokens and token_source (telemetry or estimate) for each run."
    after:
      - "Load assets/context-envelope.schema.json when validating envelope keys/version before unpack output is trusted."
      - "Load references/restore-playbook.md when unpack returns status error or schema/hash checks fail."
      - "Run deterministic checks in scripts first; only then emit restored context block."
      - "Record provenance notes from eval iterations (e.g., baseline mismatch, assertion drift, telemetry gaps) in workflow guidance."
      - "Prioritize discriminative eval assertions (exact_json/ordered_markers/regex_match) and prune always-pass checks each iteration."
      - "Capture real token telemetry in timing artifacts when available; label estimates explicitly when telemetry is unavailable."
      - "Keep generated instructions mode-neutral; avoid style-coupled phrases unless capability depends on them."
      - "Require eval harness to record both total_tokens and token_source (telemetry or estimate) for each run."
      - "Require each eval iteration to include at least one ambiguity/stress prompt with exact-value assertions."
    rationale: "Maintains discriminative signal and prevents regression to low-value assertion sets."
    status: "approved"
