# Cantrips script defaults
# Override at runtime: NPX_ARGS="-g -y" ./scripts/skills.sh add foo/bar my-skill

SKILLS_PKG="skills@1.5.7"
INSTALL_TARGET=-g
INSTALL_AGENTS=
INSTALL_FLAT=
NPX_ARGS="-g -a opencode -a claude-code -y"